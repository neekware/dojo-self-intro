# 02.00.00 — STT / Talk to Dojo — **LOCKED**

## Role in the chapter
**Input:** talk to Dojo instead of typing.  
Next: `02.10.00-tts-listen` — **Output:** let Dojo talk back, because eyes and ears are built for the world—not walls of text.

## Section master (single clip = both ends)
`video/final/LOCKED-02-stt.mp4` (~15.7s) — zoom **into IDE** → dissolve → zoom **out to mic**, full VO.  
This IS the standalone. Silent picture parked in `video/work/`.

## Story (two beats)
**A — No IDE required**  
Picture: Dojo Solo | Duo empty workspace (real product UI).  
You don’t need an IDE. Dojo Solo can do it for you.

**B — Stay in the IDE, but talk**  
Picture: Dojo-branded coding IDE on monitor (no competitor names).  
You can stay inside an IDE — but don’t be bound to the keyboard.  
Talk to Dojo. Typing is yesterday. Voice is the way forward. Grab the mic.

## Stills (locked picture sources)
| Beat | Path |
|------|------|
| A Solo/Duo | `after/final/dojo-solo-duo-LOCKED.png` (or `.jpg`) |
| B IDE | `after/final/code-ide-dojo-LOCKED.png` · `.jpg` |

Old competitor IDE parked: `after/work/code-ide-still-OLD-competitors.jpg`

## VO draft (Eve next)
> You don’t need an IDE.  
> Dojo Solo can build it with you.  
>
> Prefer your editor? Stay.  
> But don’t be bound to the keyboard and the screen.  
>
> Talk to Dojo.  
> Typing is yesterday.  
> Grab the mic — that’s the future.

## Picture (stitched draft)
`video/clips/03-ide-in-mic-out.mp4` (~13.6s)  
1. Orbit into IDE, **end-zoomed past the bezel** (full-bleed IDE)  
2. 0.45s soft dissolve  
3. Mic **reverse** — full Solo/Duo pulls out to him on the microphone (voice > typing)

Pieces:  
- `video/clips/01-orbit-into-ide.mp4` (IDE push, zoomed, no bezel)  
- `video/clips/02-mic-orbit-into-soloduo.mp4` + `-REVERSE.mp4`  

Zoom = supersampled + integer-centered (no shake).

## Section master (VO)
`video/final/LOCKED-02-stt.mp4` (~17.5s)  
- Part 1 over IDE push-in (0.4s in)  
- pause  
- Part 2 over mic pull-out (~7.3s in)  
Rebuild: `python3 scripts/build-code-section.py`

## VO (Eve)
**Part 1 (active, short — fits push):**
> Engineers write software with editors and keyboards — tethered to the keys. Powerful, but not natural.

**Part 1 (long alt, tracked):** `audio/final/code-vo-part1-long.mp3`
> Today, engineers use editors and keyboards to write software — tethered to their fingers, bound to the keys. Powerful… but it isn't natural.

**Part 2 (active, short):**
> Talking is the natural way. Just talk to Dojo, like a teammate. Break away from the ways of the past, and embrace the future.

**Part 2 (long alt, tracked):** `audio/final/code-vo-part2-long.mp3`

Picture motion is slowed (~1.15×) to end with the VO — no frozen hold on his face.

## Next
1. Approve VO sync  
2. Standalone lock + cumulative preview  
3. Then **Lane Assist** after code

## Placement
After What is Dojo · before Lane Assist (`16.00.00`).
