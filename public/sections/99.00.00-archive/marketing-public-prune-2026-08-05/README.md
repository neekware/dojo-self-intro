# Marketing Public Asset Archive — 2026-08-05

This archive preserves files removed from `dojo-marketing/public/` because they were not directly referenced by the runtime application, static metadata, CSS, manifests, or framework conventions at marketing commit `2d8fe1d`.

## Scope

- Archived files: **114**
- Logical bytes: **207,207,527**
- Original relative paths are preserved under `files/`.
- `MANIFEST.tsv` records original logical byte size and path.
- `SHA256SUMS` verifies the archived copies.

## Restore

Copy any needed file from `files/<relative-path>` back to `dojo-marketing/public/<relative-path>`, then add an explicit runtime reference and tests before shipping it again.

## Validation after removal

- Marketing public tree reduced from 166 files to 52.
- TypeScript typecheck passed.
- Full Vitest suite passed: 21 files, 62 tests.
- Next.js production build completed successfully.
